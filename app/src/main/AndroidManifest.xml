<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coinsmart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoinSmart">
        <activity
            android:name=".FinancijskiPlanActivity"
            android:exported="false" />
        <activity
            android:name=".Pitanje"
            android:exported="false" />
        <activity
            android:name=".Tema"
            android:exported="false" />
        <activity android:name=".FinancijskaPismenostActivity" />
        <activity android:name=".VidjetiDetaljeActivity" />
        <activity android:name=".PrikazTroskovaActivity" />
        <activity android:name=".UnosTroskovaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>